version: '3.8'

services:
  mongo:
    image: mongo:latest
    container_name: codeweave-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    networks:
      - sandbox-network

  redis:
    image: redis:7-alpine
    container_name: codeweave-redis
    ports:
      - "6379:6379"
    networks:
      - sandbox-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: codeweave-backend
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - MONGODB_URI=mongodb://mongo:27017/codeweave-sandbox
      - JWT_SECRET=compose_jwt_secret_phrase_here
      - GOOGLE_AI_KEY=your_gemini_api_key_here
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      - mongo
      - redis
    networks:
      - sandbox-network

networks:
  sandbox-network:
    driver: bridge

volumes:
  mongo-data:
    driver: local
